关键字 Keyword keyword key word:
活动目录 AD Active Directory Windows LDAP

在Windows server 2008 R2上配置AD的步骤：
1. 运行 一个程序，安装AD （参考百度）
2. 在开始菜单里打开 管理工具，活动目录用户和计算机，添加用户和组

注意：
先配置好AD，再安装Symphony，如果次序颠倒的话，会导致Symphony ego service无法启动（管理员密码在配置好AD后会变更）

我已经配置好的环境：
在self service上 ， yhw08c, 密码是默认密码后加！

公司内部的AD和Linux LDAP不是一套
AD的域名是ENG （不要写eng.platformlab.ibm.com）
yuhuixa 密码默认
